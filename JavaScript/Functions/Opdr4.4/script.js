
var names = (Esther, Madhwi, Nathan);
var names = document.get

function nameChecker(names) {
    // Use DOM to get the input value and save it in the above var 
  if (names == Esther || names == Madhwi || names == Nathan) {
    console.log("Welcome " + names) 
  } else { console.log("Names no recognized") }

};




// function placeResults(); {

// }